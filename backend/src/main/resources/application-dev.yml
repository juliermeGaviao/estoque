spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:mysql://localhost:3306/estoque2?useSSL=false&serverTimezone=America/Sao_Paulo
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: estoque
    password: S3nh4F0rt3.


  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false

cors:
  allowed-origins: http://localhost:5173
