spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:mysql://localhost:3306/estoque2?useSSL=false&serverTimezone=America/Sao_Paulo&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: estoque
    password: S3nh4F0rt3.


  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false

cors:
  allowed-origins: https://chocolate-se.com.br
